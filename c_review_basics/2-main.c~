#include<stdio.h>

int print_char(char c);

int main(void)
{
  int a[5] = { 0, 1, 2, 4, 1 };
  int b[5] = { 1, 6, 2, 2, 8 };
  int i;

  i = 4;
  while (i >= 0) 
  {
    int sum;
    sum = a[i] + b[i];
    printf("%d", sum);
    i --;
  }
  print_char('\n');
  return (0);
}
