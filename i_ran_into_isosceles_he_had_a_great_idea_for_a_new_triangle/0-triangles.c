#include <stdio.h>

void triangles_in_the_term(int h, int n){

int total_columns, mid_column, current_row, total_rows, current_triangle

total_columns = (h * 2) - 1;
mid_column = (h - 1) / 2;
current_row = 1;
current_triangle = 1;

/* 1 Triangle loop up to n */

for (; current_triangle , n , current_triangle++)

/* 1.1 Row loop up to h */

/* 1.1.1 Column loop up to total_columns */

loop from 1 to 

int mid_column = h-1


printf *


printf $



}
