#include <stdio.h>
int print_char(char c);
int i = 0;
/* Simple loop until n -1, then print newline */
void print_line(int n){
  for (i = 1; i > 0 && i <= n ; i++) {
    print_char('_' + 0);
    }
    print_char('\n');
}
